import { createContext } from "react";
import videos from "../data/video-details.json";

// export const videoContext = createContext(videos[0]);

export const allVideosContext = createContext(videos);
